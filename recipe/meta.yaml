{% set name = "nvector" %}
{% set version = "1.0.4" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/nvector-{{ version }}.tar.gz
  sha256: 1a9b5a62e98070e0cce8c174f3b54f207af7ae33d73d77eed65ac3d5fa64996c

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python {{ python_min }}
    - pdm-backend
    - pip
  run:
    - python >={{ python_min }}
    - numpy >=1.11.0
    - scipy >=1.3.0
    - karney >=1.0

test:
  imports:
    - nvector
  commands:
    - pip check
  requires:
    - python {{ python_min }}
    - pip

about:
  home: https://github.com/pbrod/nvector/
  summary: The nvector library is a suite of tools written in Python to solve geographical position calculations forked from nvector.
  license: BSD-2-Clause
  license_file: LICENSE.txt

extra:
  recipe-maintainers:
    - opoplawski
